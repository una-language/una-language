=-> 'ramda' R
= electronics ::
  :
    title ' iPhone '
    type 'phone'

= phones |>
  electronics
  R.find 
    R.propEq 'type' 'phone'
  R.prop 'title'
  R.toUpper
  R.trim

console.log phones

= sum <| (:: 1 2 3)
  .map (-> x (+ x 1))
  .filter (-> x (> x 2))
  .reduce (-> (x y) (+ x y)) 0

console.log sum
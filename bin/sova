#!/bin/sh

input_directory=$1
output_directory="output"

# Clean output directory
rm -rf $output_directory

# Copy all non-sova files to directories
rsync -R $(find $input_directory -type f -not -name "*.sv") $output_directory
